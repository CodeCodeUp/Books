# 基于协同过滤与内容特征的图书馆资源智能化推荐系统的设计与实现

---

## 第4章 系统实现

### 4.1 开发环境与工具

#### 4.1.1 开发环境配置

本系统的开发环境分为前端、后端和算法服务三个部分，各自使用不同的技术栈和工具。

**（1）前端开发环境**

操作系统：Windows 11专业版。

Node.js版本：v18.16.0。Node.js是JavaScript运行时环境，提供npm包管理器，用于安装和管理前端依赖。通过官网下载安装包安装，配置环境变量后可在命令行使用node和npm命令。

开发工具：Visual Studio Code 1.85.0。VS Code是轻量级代码编辑器，支持JavaScript、Vue、HTML、CSS等语言。安装了Vetur插件（Vue语法高亮和智能提示）、ESLint插件（代码规范检查）、Prettier插件（代码格式化）。

浏览器：Chrome 120.0。Chrome浏览器的开发者工具提供了网络监控、控制台、元素检查等功能，便于前端调试。

包管理器：npm 9.5.1。npm是Node.js的默认包管理器，用于安装Vue、Element Plus等依赖包。执行npm install命令安装package.json中定义的所有依赖。

构建工具：Vite 4.4.9。Vite是新一代前端构建工具，开发服务器启动快，热更新迅速。执行npm run dev启动开发服务器，执行npm run build打包生产代码。

**（2）后端开发环境**

操作系统：Windows 11专业版。

Java版本：OpenJDK 17.0.2。Java 17是长期支持版本，SpringBoot 3要求Java 17以上。通过AdoptOpenJDK下载安装，配置JAVA_HOME环境变量。

开发工具：IntelliJ IDEA 2023.2.1 Ultimate。IDEA是专业的Java IDE，提供代码补全、重构、调试、Maven集成等功能。安装了Spring Boot插件，便于SpringBoot项目开发。

构建工具：Maven 3.9.0。Maven是Java项目的依赖管理和构建工具，通过pom.xml文件管理SpringBoot、MyBatis Plus等依赖。执行mvn clean install编译打包项目。

数据库客户端：Navicat Premium 16.0。Navicat是数据库管理工具，支持MySQL、PostgreSQL等多种数据库。用于连接远程MySQL数据库，执行SQL查询，导入导出数据，查看表结构。

API测试工具：Postman 10.18。Postman用于测试RESTful API，发送HTTP请求，查看响应结果。测试用户注册、登录、图书查询、推荐调用等接口，验证功能正确性。

**（3）算法服务开发环境**

操作系统：Windows 11专业版。

Python版本：Python 3.10.5。Python是算法服务的开发语言，3.10版本语法稳定，性能优化良好。通过官网下载安装包安装，配置环境变量。

开发工具：PyCharm 2023.2.1 Professional。PyCharm是专业的Python IDE，提供代码补全、调试、虚拟环境管理、包管理等功能。创建Flask项目，配置虚拟环境，安装依赖包。

虚拟环境：venv。虚拟环境隔离项目依赖，避免不同项目的包版本冲突。在项目目录下执行python -m venv venv创建虚拟环境，激活后使用pip安装依赖。

依赖包管理：pip 23.0。pip是Python的包管理器，执行pip install -r requirements.txt安装Flask、NumPy、Pandas等依赖包。requirements.txt文件列出了所有依赖及其版本号。

**（4）数据库环境**

数据库服务器：远程MySQL服务器，地址116.205.244.106，端口3306。

MySQL版本：MySQL 8.0.30。

数据库名称：book_recommendation。

字符集：utf8mb4，支持中文和特殊字符存储。

存储引擎：InnoDB，支持事务和外键，保证数据一致性。

访问方式：本地开发环境通过网络连接远程数据库，使用用户名root和密码202358hjq。生产环境建议创建专用用户，限制权限，提升安全性。

#### 4.1.2 项目初始化

**（1）前端项目初始化**

步骤1：使用Vite创建Vue 3项目。执行命令：

```bash
npm create vite@latest book-recommendation-frontend -- --template vue
```

选择Vue模板，生成项目骨架，包括src目录、public目录、index.html、package.json等文件。

步骤2：安装依赖。进入项目目录，执行：

```bash
npm install
```

安装Vue 3、Vite等基础依赖。

步骤3：安装Element Plus、Pinia、Vue Router等库。执行：

```bash
npm install element-plus @element-plus/icons-vue
npm install pinia
npm install vue-router@4
npm install axios
```

步骤4：配置Vite。编辑vite.config.js，配置代理解决跨域问题：

```javascript
export default {
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
}
```

步骤5：创建项目结构。在src目录下创建views（页面组件）、components（公共组件）、router（路由配置）、stores（状态管理）、api（API接口）、utils（工具函数）等子目录。

**（2）后端项目初始化**

步骤1：使用Spring Initializr创建SpringBoot项目。访问https://start.spring.io/，选择Maven项目、Java 17、SpringBoot 3.1.5，添加依赖Spring Web、Spring Security、MyBatis Framework、MySQL Driver、Lombok。下载生成的项目压缩包，解压。

步骤2：导入项目到IDEA。打开IDEA，选择Open，导入项目。IDEA自动识别为Maven项目，下载依赖。

步骤3：配置application.yml。在src/main/resources目录下创建application.yml，配置数据库连接、服务器端口、JWT密钥等：

```yaml
spring:
  datasource:
    url: jdbc:mysql://116.205.244.106:3306/book_recommendation?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 202358hjq
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

jwt:
  secret: your-secret-key-here
  expiration: 86400000  # 24小时
```

步骤4：创建项目结构。在src/main/java/com/bookrs/recommendation目录下创建controller、service、mapper、entity、config、common等包。

**（3）算法服务项目初始化**

步骤1：创建项目目录。在项目根目录下创建recommendation-algorithm-service文件夹。

步骤2：创建虚拟环境。进入目录，执行：

```bash
python -m venv venv
```

激活虚拟环境（Windows下执行venv\Scripts\activate）。

步骤3：创建requirements.txt，列出依赖：

```
Flask==2.3.0
Flask-CORS==4.0.0
numpy==1.24.3
pandas==2.0.2
scikit-learn==1.3.0
PyMySQL==1.1.0
SQLAlchemy==2.0.15
```

步骤4：安装依赖。执行：

```bash
pip install -r requirements.txt
```

步骤5：创建项目结构。创建algorithms、data、utils等包，创建app.py主文件、config.py配置文件。

### 4.2 数据预处理实现

数据预处理是推荐系统的基础工作，决定了算法输入数据的质量。本系统使用公开的Book-Crossing数据集，需要对原始数据进行清洗、转换和导入。

#### 4.2.1 数据获取

Book-Crossing数据集由德国弗莱堡大学Cai-Nicolas Ziegler收集，包含真实的图书评分数据。数据以CSV格式发布，分为三个文件：

BX-Book-Ratings.csv：评分数据，包含User-ID、ISBN、Book-Rating三列。原始数据约115万条评分记录。

BX-Books.csv：图书数据，包含ISBN、Book-Title、Book-Author、Year-Of-Publication、Publisher、Image-URL-S、Image-URL-M、Image-URL-L八列。约27万本图书。

BX-Users.csv：用户数据，包含User-ID、Location、Age三列。约28万用户。

数据下载：编写Python脚本download_book_crossing.py，从镜像网站下载数据集压缩包BX-CSV-Dump.zip，解压到data/book_crossing目录。

```python
import requests
import zipfile
import os

def download_dataset():
    url = "https://cdn.freecodecamp.org/project-data/books/book-crossings.zip"
    zip_path = "data/BX-CSV-Dump.zip"

    # 下载数据集
    response = requests.get(url, stream=True)
    with open(zip_path, 'wb') as f:
        for chunk in response.iter_content(chunk_size=8192):
            f.write(chunk)

    # 解压文件
    with zipfile.ZipFile(zip_path, 'r') as zip_ref:
        zip_ref.extractall("data/book_crossing/")
```

#### 4.2.2 评分数据清洗

原始评分数据存在两类问题：一是包含隐式评分（评分值为0），隐式评分表示用户浏览或购买了图书，但未给出明确评价，信息量低；二是评分范围为0-10分，需要标准化为1-5分制。

清洗脚本basic_data_cleaning.py的关键代码：

```python
import pandas as pd

# 读取评分数据
ratings_df = pd.read_csv('data/book_crossing/BX-Book-Ratings.csv',
                         sep=';', encoding='latin-1',
                         names=['user_id', 'isbn', 'rating'])

# 过滤隐式评分
explicit_ratings = ratings_df[ratings_df['rating'] > 0].copy()

# 标准化评分到1-5分制
# 原始0-10分映射到1-5分：1-2分映射到1分，3-4分映射到2分，以此类推
explicit_ratings['rating'] = (explicit_ratings['rating'] / 2).round()
explicit_ratings['rating'] = explicit_ratings['rating'].clip(1, 5)

# 过滤活跃用户：至少评分5次
user_counts = explicit_ratings['user_id'].value_counts()
active_users = user_counts[user_counts >= 5].index
filtered_ratings = explicit_ratings[explicit_ratings['user_id'].isin(active_users)]

# 过滤热门图书：至少被3个用户评分
book_counts = filtered_ratings['isbn'].value_counts()
popular_books = book_counts[book_counts >= 3].index
final_ratings = filtered_ratings[filtered_ratings['isbn'].isin(popular_books)]

# 保存清洗后的数据
final_ratings.to_csv('processed_data/ratings_cleaned.csv', index=False)
```

清洗结果：原始115万条评分，过滤隐式评分后约43万条显式评分，再过滤低活跃用户和冷门图书后，最终保留433,671条高质量评分。

#### 4.2.3 图书数据清洗

图书数据存在以下问题：CSV格式错误（书名中包含分隔符导致列错位）、出版年份异常（如年份为0或超过当前年份）、缺失值较多。

清洗步骤：

步骤1：修复CSV格式。原始文件使用分号分隔，但部分书名包含分号，导致列错位。采用自定义解析器处理：

```python
import csv

def parse_books_csv(file_path):
    books = []
    with open(file_path, 'r', encoding='latin-1', errors='ignore') as f:
        reader = csv.reader(f, delimiter=';', quotechar='"')
        next(reader)  # 跳过表头

        for row in reader:
            if len(row) >= 8:  # 确保列数完整
                isbn = row[0].strip('"')
                title = row[1].strip('"')
                author = row[2].strip('"')
                year = row[3].strip('"')
                publisher = row[4].strip('"')
                image_s = row[5].strip('"')
                image_m = row[6].strip('"')
                image_l = row[7].strip('"')

                books.append({
                    'isbn': isbn,
                    'title': title,
                    'author': author,
                    'year': year,
                    'publisher': publisher,
                    'image_url_s': image_s,
                    'image_url_m': image_m,
                    'image_url_l': image_l
                })

    return pd.DataFrame(books)
```

步骤2：清洗出版年份。将非数字年份、异常年份（<1900或>2023）替换为空值：

```python
def clean_year(year_str):
    try:
        year = int(year_str)
        if 1900 <= year <= 2023:
            return year
        else:
            return None
    except:
        return None

books_df['year'] = books_df['year'].apply(clean_year)
```

步骤3：处理缺失值。作者、出版社为空时填充为"Unknown"：

```python
books_df['author'].fillna('Unknown', inplace=True)
books_df['publisher'].fillna('Unknown', inplace=True)
```

步骤4：去重。根据ISBN去重，保留第一条记录：

```python
books_df.drop_duplicates(subset='isbn', keep='first', inplace=True)
```

清洗结果：保留271,360本图书的完整信息。

#### 4.2.4 用户数据处理

用户数据包含User-ID、Location、Age三列。处理目标是提取年龄组和国家信息，用于内容推荐。

处理步骤：

步骤1：清洗年龄数据。年龄范围应在1-120之间，超出范围的视为异常值：

```python
users_df = pd.read_csv('data/book_crossing/BX-Users.csv', sep=';', encoding='latin-1')

def clean_age(age):
    try:
        age = int(age)
        if 1 <= age <= 120:
            return age
        else:
            return None
    except:
        return None

users_df['Age'] = users_df['Age'].apply(clean_age)
```

步骤2：划分年龄组。根据年龄值划分为标准年龄段：

```python
def assign_age_group(age):
    if pd.isna(age):
        return None
    if age < 18:
        return 'Under 18'
    elif age < 25:
        return '18-24'
    elif age < 35:
        return '25-34'
    elif age < 45:
        return '35-44'
    elif age < 55:
        return '45-54'
    else:
        return '55+'

users_df['age_group'] = users_df['Age'].apply(assign_age_group)
```

步骤3：提取国家信息。Location字段格式为"城市, 州/省, 国家"，提取最后一部分作为国家：

```python
def extract_country(location):
    if pd.isna(location):
        return None
    parts = location.split(',')
    if len(parts) >= 3:
        country = parts[-1].strip().lower()
        return country
    else:
        return None

users_df['country'] = users_df['Location'].apply(extract_country)
```

步骤4：过滤活跃用户。只保留在清洗后评分数据中出现的用户：

```python
# 读取清洗后的评分数据，获取用户ID列表
active_user_ids = pd.read_csv('processed_data/ratings_cleaned.csv')['user_id'].unique()

# 过滤用户数据
active_users_df = users_df[users_df['User-ID'].isin(active_user_ids)]
```

处理结果：保留77,805名活跃用户的人口统计信息。

#### 4.2.5 数据导入数据库

清洗后的数据需要导入到MySQL数据库，供后端和算法服务使用。导入脚本final_import.py的实现：

```python
import pandas as pd
import pymysql
from sqlalchemy import create_engine

# 创建数据库连接
engine = create_engine('mysql+pymysql://root:202358hjq@116.205.244.106:3306/book_recommendation')

# 导入图书数据
books_df = pd.read_csv('processed_data/books_metadata.csv')
books_df.rename(columns={'isbn': 'book_id'}, inplace=True)
books_df.to_sql('books', con=engine, if_exists='append', index=False)

# 导入用户数据
users_df = pd.read_csv('processed_data/users_metadata.csv')
users_df.to_sql('users', con=engine, if_exists='append', index=False)

# 导入评分数据
ratings_df = pd.read_csv('processed_data/ratings_cleaned.csv')
ratings_df.rename(columns={'isbn': 'book_id'}, inplace=True)
ratings_df.to_sql('ratings', con=engine, if_exists='append', index=False, chunksize=10000)
```

导入注意事项：
- 评分数据量大（43万条），使用chunksize参数分批插入，避免内存溢出。
- 图书表以ISBN作为主键，需要确保数据无重复。
- 用户表的user_id字段设置为自增，从300001开始，与原始数据区分。
- 导入前执行数据库修复脚本V1.1_Remove_Foreign_Keys.sql，移除外键约束，避免导入失败。

导入后验证：执行SQL查询，确认数据量和数据质量：

```sql
SELECT COUNT(*) FROM users;   -- 应返回77,805
SELECT COUNT(*) FROM books;   -- 应返回271,360
SELECT COUNT(*) FROM ratings; -- 应返回433,671
```

### 4.3 推荐算法实现

推荐算法是系统的核心，本节详细介绍用户协同过滤、物品协同过滤、内容推荐和混合推荐的代码实现。

#### 4.3.1 数据加载模块实现

数据加载模块负责从MySQL数据库读取评分、图书、用户数据，转换为Pandas DataFrame格式供算法使用。代码位于data/data_loader.py。

```python
import pandas as pd
import pymysql
from sqlalchemy import create_engine
import logging

logger = logging.getLogger(__name__)

class DataLoader:
    def __init__(self):
        self.db_config = {
            'host': '116.205.244.106',
            'port': 3306,
            'user': 'root',
            'password': '202358hjq',
            'database': 'book_recommendation',
            'charset': 'utf8mb4'
        }
        self.engine = None
        self.last_update_time = None
        self.ratings_df = None
        self.books_df = None
        self.users_df = None

    def initialize_data(self):
        """启动时全量加载数据"""
        logger.info("开始全量加载数据...")
        self.engine = create_engine(
            f"mysql+pymysql://{self.db_config['user']}:{self.db_config['password']}"
            f"@{self.db_config['host']}:{self.db_config['port']}/{self.db_config['database']}"
        )

        # 加载评分数据
        ratings_query = "SELECT user_id, book_id, rating FROM ratings WHERE rating > 0"
        self.ratings_df = pd.read_sql(ratings_query, self.engine)
        logger.info(f"加载评分数据: {len(self.ratings_df)} 条")

        # 加载图书数据
        books_query = "SELECT book_id, title, author, publisher, year, avg_rating, rating_count FROM books"
        self.books_df = pd.read_sql(books_query, self.engine)
        logger.info(f"加载图书数据: {len(self.books_df)} 本")

        # 加载用户数据
        users_query = "SELECT user_id, age, age_group, country FROM users"
        self.users_df = pd.read_sql(users_query, self.engine)
        logger.info(f"加载用户数据: {len(self.users_df)} 人")

        self.last_update_time = pd.Timestamp.now()
        logger.info("数据加载完成")

    def get_ratings_data(self, force_refresh=False):
        """获取评分数据，支持增量更新"""
        if force_refresh or self.ratings_df is None:
            # 强制刷新或首次加载
            ratings_query = "SELECT user_id, book_id, rating FROM ratings WHERE rating > 0"
            self.ratings_df = pd.read_sql(ratings_query, self.engine)
        else:
            # 增量更新：查询上次更新后的新评分
            query = f"""
            SELECT user_id, book_id, rating
            FROM ratings
            WHERE rating > 0 AND created_at > '{self.last_update_time}'
            """
            new_ratings = pd.read_sql(query, self.engine)

            if not new_ratings.empty:
                # 合并新评分，去重（用户对同一图书多次评分保留最新）
                self.ratings_df = pd.concat([self.ratings_df, new_ratings])
                self.ratings_df.drop_duplicates(subset=['user_id', 'book_id'], keep='last', inplace=True)
                logger.info(f"增量更新评分数据: {len(new_ratings)} 条新评分")
                self.last_update_time = pd.Timestamp.now()

        return self.ratings_df

    def get_books_data(self, force_refresh=False):
        """获取图书数据"""
        if force_refresh or self.books_df is None:
            books_query = "SELECT book_id, title, author, publisher, year, avg_rating, rating_count FROM books"
            self.books_df = pd.read_sql(books_query, self.engine)
        return self.books_df

    def get_users_data(self):
        """获取用户数据"""
        if self.users_df is None:
            users_query = "SELECT user_id, age, age_group, country FROM users"
            self.users_df = pd.read_sql(users_query, self.engine)
        return self.users_df
```

设计要点：
- initialize_data方法在服务启动时调用，全量加载数据到内存，避免后续频繁数据库查询。
- get_ratings_data支持增量更新，查询上次更新时间后的新评分，合并到已有数据并去重。
- force_refresh参数允许强制刷新数据，用于用户评分后需要立即获取最新评分的场景。
- 使用SQLAlchemy的create_engine创建连接池，提升并发查询性能。

#### 4.3.2 用户协同过滤算法实现

用户协同过滤算法的完整代码位于algorithms/collaborative_filtering.py，核心方法包括相似用户发现、评分预测、推荐生成。

```python
import numpy as np
import pandas as pd
from sklearn.metrics.pairwise import cosine_similarity
import logging

logger = logging.getLogger(__name__)

class UserBasedCollaborativeFiltering:
    def __init__(self):
        self.data_loader = DataLoader()
        self.cache = RecommendationCache()
        self.ratings_df = None
        self.books_df = None

    def load_data(self):
        """加载数据"""
        if self.ratings_df is None:
            self.data_loader.initialize_data()
        self.ratings_df = self.data_loader.get_ratings_data()
        self.books_df = self.data_loader.get_books_data()

    def find_similar_users_efficient(self, target_user_id, min_common_items=2, top_k=50):
        """高效查找相似用户"""
        # 获取目标用户的评分记录
        target_ratings = self.ratings_df[self.ratings_df['user_id'] == target_user_id]

        if target_ratings.empty:
            logger.warning(f"用户 {target_user_id} 没有评分记录")
            return []

        target_books = set(target_ratings['book_id'].values)
        target_ratings_dict = dict(zip(target_ratings['book_id'], target_ratings['rating']))

        # 候选用户预筛选：找评分过相同图书的用户
        common_users_df = self.ratings_df[
            self.ratings_df['book_id'].isin(target_books) &
            (self.ratings_df['user_id'] != target_user_id)
        ]

        # 按用户分组，计算共同评分图书数量
        common_counts = common_users_df.groupby('user_id')['book_id'].count()
        qualified_users = common_counts[common_counts >= min_common_items].index

        logger.info(f"候选用户数量: {len(qualified_users)}")

        # 计算相似度
        similarities = []
        for other_user_id in qualified_users:
            if len(similarities) >= top_k * 3:
                break

            similarity = self._calculate_user_similarity(
                target_user_id, other_user_id, target_ratings_dict
            )

            if similarity > 0.3:  # 相似度阈值
                similarities.append({
                    'user_id': other_user_id,
                    'similarity': similarity
                })

        # 排序
        similarities.sort(key=lambda x: x['similarity'], reverse=True)
        return similarities[:top_k]

    def _calculate_user_similarity(self, user1_id, user2_id, user1_ratings_dict):
        """计算两个用户的余弦相似度"""
        user2_ratings = self.ratings_df[self.ratings_df['user_id'] == user2_id]
        user2_ratings_dict = dict(zip(user2_ratings['book_id'], user2_ratings['rating']))

        # 找共同评分的图书
        common_books = set(user1_ratings_dict.keys()) & set(user2_ratings_dict.keys())

        if len(common_books) < 2:
            return 0.0

        # 构建评分向量
        vector1 = np.array([user1_ratings_dict[book] for book in common_books])
        vector2 = np.array([user2_ratings_dict[book] for book in common_books])

        # 计算余弦相似度
        similarity = cosine_similarity([vector1], [vector2])[0][0]
        return float(similarity)

    def get_recommendations(self, user_id, top_n=10, min_rating=3.0):
        """生成推荐"""
        # 检查缓存
        cached = self.cache.get_cached_recommendations(user_id)
        if cached:
            logger.info(f"返回缓存的推荐结果")
            return cached['recommendations'][:top_n]

        # 查找相似用户
        similar_users = self.find_similar_users_efficient(user_id)

        if not similar_users:
            logger.warning(f"未找到相似用户，返回空推荐")
            return []

        # 收集候选图书
        user_rated_books = set(
            self.ratings_df[self.ratings_df['user_id'] == user_id]['book_id'].values
        )

        candidate_books = {}

        for similar_user in similar_users[:20]:  # 取前20个相似用户
            similar_user_id = similar_user['user_id']
            similarity = similar_user['similarity']

            # 获取相似用户的高分图书（评分>=3.0）
            similar_user_ratings = self.ratings_df[
                (self.ratings_df['user_id'] == similar_user_id) &
                (self.ratings_df['rating'] >= 3.0)
            ]

            for _, row in similar_user_ratings.iterrows():
                book_id = row['book_id']
                rating = row['rating']

                if book_id not in user_rated_books:
                    if book_id not in candidate_books:
                        candidate_books[book_id] = []
                    candidate_books[book_id].append((similarity, rating))

        # 预测评分
        predictions = []
        for book_id, ratings_data in candidate_books.items():
            # 加权平均
            weighted_sum = sum(sim * rating for sim, rating in ratings_data)
            similarity_sum = sum(sim for sim, rating in ratings_data)

            predicted_rating = weighted_sum / similarity_sum if similarity_sum > 0 else 0

            if predicted_rating >= min_rating:
                predictions.append({
                    'book_id': book_id,
                    'predicted_rating': predicted_rating
                })

        # 排序
        predictions.sort(key=lambda x: x['predicted_rating'], reverse=True)

        # 查询图书详情
        recommendations = []
        for pred in predictions[:top_n]:
            book_info = self.books_df[self.books_df['book_id'] == pred['book_id']]
            if not book_info.empty:
                book = book_info.iloc[0]
                recommendations.append({
                    'bookId': pred['book_id'],
                    'title': book['title'],
                    'author': book['author'],
                    'predictedRating': round(pred['predicted_rating'], 2),
                    'algorithm': 'user_based_cf'
                })

        # 缓存结果
        self.cache.save_recommendations(user_id, recommendations, 'user_based_cf')

        return recommendations
```

关键优化：
- find_similar_users_efficient通过共同评分图书索引预筛选候选用户，避免全量计算。
- 使用NumPy和scikit-learn的cosine_similarity计算余弦相似度，性能优于纯Python循环。
- 只收集相似用户的高分图书（≥3.0分）作为候选，减少预测计算量。
- 推荐结果缓存到文件，有效期1小时，减少重复计算。

#### 4.3.3 缓存模块实现

缓存模块负责推荐结果的读取、写入、失效和清除。代码位于utils/cache.py。

```python
import os
import json
import time
import logging

logger = logging.getLogger(__name__)

class RecommendationCache:
    def __init__(self, cache_dir='models/cache', cache_duration=3600):
        self.cache_dir = cache_dir
        self.cache_duration = cache_duration  # 缓存有效期（秒）

        # 创建缓存目录
        if not os.path.exists(cache_dir):
            os.makedirs(cache_dir)

    def get_cached_recommendations(self, user_id):
        """获取缓存的推荐结果"""
        cache_file = os.path.join(self.cache_dir, f"user_{user_id}_recommendations.json")

        if not os.path.exists(cache_file):
            return None

        try:
            with open(cache_file, 'r', encoding='utf-8') as f:
                cache_data = json.load(f)

            # 检查缓存是否过期
            cache_time = cache_data.get('timestamp', 0)
            if time.time() - cache_time > self.cache_duration:
                logger.info(f"用户 {user_id} 的缓存已过期")
                return None

            logger.info(f"命中缓存: 用户 {user_id}")
            return cache_data

        except Exception as e:
            logger.error(f"读取缓存失败: {e}")
            return None

    def save_recommendations(self, user_id, recommendations, algorithm):
        """保存推荐结果到缓存"""
        cache_file = os.path.join(self.cache_dir, f"user_{user_id}_recommendations.json")

        cache_data = {
            'user_id': user_id,
            'recommendations': recommendations,
            'algorithm': algorithm,
            'timestamp': time.time()
        }

        try:
            with open(cache_file, 'w', encoding='utf-8') as f:
                json.dump(cache_data, f, ensure_ascii=False, indent=2)
            logger.info(f"缓存推荐结果: 用户 {user_id}, 数量 {len(recommendations)}")
        except Exception as e:
            logger.error(f"保存缓存失败: {e}")

    def invalidate_user_cache(self, user_id):
        """清除用户的推荐缓存"""
        cache_file = os.path.join(self.cache_dir, f"user_{user_id}_recommendations.json")

        if os.path.exists(cache_file):
            os.remove(cache_file)
            logger.info(f"已清除用户 {user_id} 的缓存")
```

设计特点：
- 缓存以JSON格式存储在文件系统，包含推荐列表、算法类型、时间戳。
- get_cached_recommendations检查缓存文件是否存在、是否过期，过期自动失效。
- invalidate_user_cache在用户评分后调用，清除缓存，确保推荐时效性。
- 缓存有效期设为3600秒（1小时），平衡了时效性和计算成本。

#### 4.3.4 物品协同过滤算法实现

物品协同过滤算法用于计算图书间的相似度,推荐与用户已评分图书相似的其他图书。代码位于algorithms/item_based_cf.py。

```python
import numpy as np
import pandas as pd
from sklearn.metrics.pairwise import cosine_similarity
import logging

logger = logging.getLogger(__name__)

class ItemBasedCollaborativeFiltering:
    def __init__(self, shared_data_loader=None):
        if shared_data_loader:
            self.data_loader = shared_data_loader
        else:
            self.data_loader = DataLoader()
        self.cache = RecommendationCache()
        self.ratings_df = None
        self.books_df = None
        self.item_similarity_cache = {}  # 物品相似度缓存

    def load_data(self):
        """加载数据"""
        self.ratings_df = self.data_loader.get_ratings_data()
        self.books_df = self.data_loader.get_books_data()

    def calculate_item_similarity(self, item1_id, item2_id):
        """计算两个物品的余弦相似度"""
        # 获取评分过这两本书的用户
        item1_ratings = self.ratings_df[self.ratings_df['book_id'] == item1_id]
        item2_ratings = self.ratings_df[self.ratings_df['book_id'] == item2_id]

        # 找共同评分用户
        common_users = set(item1_ratings['user_id']) & set(item2_ratings['user_id'])

        if len(common_users) < 2:
            return 0.0

        # 构建评分向量
        item1_dict = dict(zip(item1_ratings['user_id'], item1_ratings['rating']))
        item2_dict = dict(zip(item2_ratings['user_id'], item2_ratings['rating']))

        vector1 = np.array([item1_dict[user] for user in common_users])
        vector2 = np.array([item2_dict[user] for user in common_users])

        # 计算余弦相似度
        similarity = cosine_similarity([vector1], [vector2])[0][0]
        return float(similarity)

    def find_similar_items(self, target_item_id, top_k=10):
        """查找相似图书"""
        # 检查缓存
        cache_key = f"item_{target_item_id}_similar"
        if cache_key in self.item_similarity_cache:
            return self.item_similarity_cache[cache_key]

        # 获取评分过目标图书的用户
        target_users = set(
            self.ratings_df[self.ratings_df['book_id'] == target_item_id]['user_id']
        )

        if not target_users:
            return []

        # 候选图书预筛选：这些用户评分过的其他图书
        candidate_items = set(
            self.ratings_df[self.ratings_df['user_id'].isin(target_users)]['book_id']
        )
        candidate_items.discard(target_item_id)

        # 计算相似度
        similarities = []
        for candidate_id in candidate_items:
            similarity = self.calculate_item_similarity(target_item_id, candidate_id)
            if similarity > 0.1:
                similarities.append({
                    'book_id': candidate_id,
                    'similarity': similarity
                })

        # 排序并缓存
        similarities.sort(key=lambda x: x['similarity'], reverse=True)
        result = similarities[:top_k]
        self.item_similarity_cache[cache_key] = result

        return result

    def get_recommendations(self, user_id, top_n=10, min_rating=3.0):
        """基于物品的协同过滤推荐"""
        if self.ratings_df is None:
            self.load_data()

        # 获取用户评分的高分图书（>=4.0分）
        user_ratings = self.ratings_df[
            (self.ratings_df['user_id'] == user_id) &
            (self.ratings_df['rating'] >= 4.0)
        ]

        if user_ratings.empty:
            logger.warning(f"用户 {user_id} 没有高分评分记录")
            return []

        user_rated_books = set(
            self.ratings_df[self.ratings_df['user_id'] == user_id]['book_id']
        )

        # 收集候选图书
        candidate_scores = {}

        for _, row in user_ratings.iterrows():
            book_id = row['book_id']
            user_rating = row['rating']

            # 查找相似图书
            similar_items = self.find_similar_items(book_id, top_k=20)

            for similar in similar_items:
                similar_book_id = similar['book_id']
                similarity = similar['similarity']

                # 跳过已评分图书
                if similar_book_id in user_rated_books:
                    continue

                # 加权累计分数
                if similar_book_id not in candidate_scores:
                    candidate_scores[similar_book_id] = {
                        'weighted_sum': 0,
                        'weight_sum': 0
                    }

                weight = similarity * user_rating
                candidate_scores[similar_book_id]['weighted_sum'] += weight
                candidate_scores[similar_book_id]['weight_sum'] += similarity

        # 计算预测评分
        predictions = []
        for book_id, scores in candidate_scores.items():
            predicted_rating = scores['weighted_sum'] / scores['weight_sum']

            if predicted_rating >= min_rating:
                predictions.append({
                    'book_id': book_id,
                    'predicted_rating': predicted_rating
                })

        # 排序
        predictions.sort(key=lambda x: x['predicted_rating'], reverse=True)

        # 查询图书详情
        recommendations = []
        for pred in predictions[:top_n]:
            book_info = self.books_df[self.books_df['book_id'] == pred['book_id']]
            if not book_info.empty:
                book = book_info.iloc[0]
                recommendations.append({
                    'bookId': pred['book_id'],
                    'title': book['title'],
                    'author': book['author'],
                    'predictedRating': round(pred['predicted_rating'], 2),
                    'algorithm': 'item_based_cf'
                })

        return recommendations
```

物品协同过滤的优势是相似度相对稳定,可以持久化缓存。缺点是对于新图书和评分少的图书,相似度计算不可靠。

#### 4.3.5 基于内容的推荐算法实现

基于内容的推荐算法利用用户人口统计学特征匹配图书特征。相关代码逻辑在混合推荐类中实现。

核心的内容匹配策略包括：

**年龄组匹配**：根据不同年龄段对图书出版年代的偏好差异,计算匹配分数。例如18岁以下青少年偏好2005年后的新书（匹配分1.0）,而55岁以上用户偏好1990年前的经典（匹配分1.0）。

**地域文化匹配**：基于用户国家与图书作者的文化关联性。英语国家用户偏好英语作者作品,欧洲用户偏好欧洲主题图书。

**图书质量匹配**：综合平均评分和评分人数,计算质量分数。公式为：

```
quality_score = (avg_rating/5.0 + min(1.0, rating_count/100)) / 2
```

最终匹配分数为三者加权和：`match_score = 0.5×年龄分 + 0.3×地域分 + 0.2×质量分`

#### 4.3.6 混合推荐策略实现

混合推荐策略组合多种算法,提供全场景覆盖。代码位于algorithms/hybrid.py。

```python
import pandas as pd
import logging

logger = logging.getLogger(__name__)

class HybridRecommendation:
    def __init__(self, shared_data_loader=None):
        self.data_loader = shared_data_loader
        self.user_cf = None
        self.item_cf = None

    def get_hybrid_user_recommendations(self, user_id, top_n=10):
        """混合用户推荐：70%协同过滤 + 30%内容推荐"""
        # 检查用户状态
        ratings_df = self.data_loader.get_ratings_data()
        users_df = self.data_loader.get_users_data()

        user_ratings = ratings_df[ratings_df['user_id'] == user_id]
        user_info = users_df[users_df['user_id'] == user_id]

        has_ratings = not user_ratings.empty
        has_features = not user_info.empty and \
                      not pd.isna(user_info.iloc[0]['age_group']) and \
                      not pd.isna(user_info.iloc[0]['country'])

        # 决策树逻辑
        if has_ratings and has_features:
            logger.info(f"用户{user_id}：混合推荐策略")

            # 70%协同过滤
            cf_recs = self.user_cf.get_recommendations(user_id, top_n=10)

            # 30%内容推荐（简化版：基于用户高分图书的同作者推荐）
            content_recs = self._get_content_recommendations(user_id, top_n=10)

            # 合并：取协同过滤前7本 + 内容推荐不重复的3本
            hybrid_recs = cf_recs[:7]
            cf_book_ids = set(r['bookId'] for r in cf_recs)
            content_additional = [r for r in content_recs if r['bookId'] not in cf_book_ids]
            hybrid_recs.extend(content_additional[:3])

            return hybrid_recs[:top_n]

        elif has_ratings:
            logger.info(f"用户{user_id}：纯协同过滤")
            return self.user_cf.get_recommendations(user_id, top_n=top_n)

        elif has_features:
            logger.info(f"用户{user_id}：纯内容推荐")
            return self._get_content_recommendations(user_id, top_n=top_n)

        else:
            logger.info(f"用户{user_id}：热门推荐（降级）")
            return self._get_popular_books(top_n)

    def _get_content_recommendations(self, user_id, top_n=10):
        """基于内容的推荐（简化实现：基于用户特征和高分图书）"""
        ratings_df = self.data_loader.get_ratings_data()
        books_df = self.data_loader.get_books_data()

        # 获取用户高分图书的作者
        user_high_rated = ratings_df[
            (ratings_df['user_id'] == user_id) &
            (ratings_df['rating'] >= 4.0)
        ]

        if user_high_rated.empty:
            return self._get_popular_books(top_n)

        # 提取喜欢的作者
        liked_books = user_high_rated.merge(books_df, on='book_id')
        favorite_authors = liked_books['author'].value_counts().head(5).index.tolist()

        # 推荐这些作者的其他高分图书
        user_rated_books = set(ratings_df[ratings_df['user_id'] == user_id]['book_id'])

        recommendations = []
        for author in favorite_authors:
            author_books = books_df[
                (books_df['author'] == author) &
                (~books_df['book_id'].isin(user_rated_books)) &
                (books_df['rating_count'] >= 5)
            ].sort_values(by='avg_rating', ascending=False).head(3)

            for _, book in author_books.iterrows():
                if len(recommendations) >= top_n:
                    break
                recommendations.append({
                    'bookId': book['book_id'],
                    'title': book['title'],
                    'author': book['author'],
                    'matchScore': 0.85,
                    'algorithm': 'content_based'
                })

        return recommendations[:top_n]

    def _get_popular_books(self, top_n=10):
        """热门图书推荐（降级方案）"""
        books_df = self.data_loader.get_books_data()

        popular = books_df[
            (books_df['rating_count'] >= 100) &
            (books_df['avg_rating'] >= 4.0)
        ].sort_values(by=['avg_rating', 'rating_count'], ascending=False)

        recommendations = []
        for _, book in popular.head(top_n).iterrows():
            recommendations.append({
                'bookId': book['book_id'],
                'title': book['title'],
                'author': book['author'],
                'avgRating': float(book['avg_rating']),
                'algorithm': 'popular_fallback'
            })

        return recommendations
```

混合推荐的核心是根据用户状态（是否有评分、是否有特征信息）动态选择算法组合,实现全场景覆盖。

### 4.4 Web后端实现

Web后端基于SpringBoot 3开发,提供RESTful API,处理业务逻辑,调用推荐算法服务。

#### 4.4.1 用户认证与授权实现

用户认证采用JWT机制。SecurityConfig配置了Spring Security拦截规则：

```java
@Configuration
@EnableWebSecurity
@RequiredArgsConstructor
public class SecurityConfig {

    private final JwtAuthenticationFilter jwtAuthenticationFilter;

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(session ->
                session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/users/register", "/users/login").permitAll()
                .requestMatchers("/books/**").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter,
                UsernamePasswordAuthenticationFilter.class);

        return http.build();
    }
}
```

JwtAuthenticationFilter拦截请求,验证Token,设置认证上下文。UserController处理用户注册和登录：

```java
@RestController
@RequestMapping("/users")
@RequiredArgsConstructor
public class UserController {

    private final UserService userService;
    private final JwtTokenUtil jwtTokenUtil;

    @PostMapping("/register")
    public Result<User> register(
            @RequestParam String username,
            @RequestParam String password,
            @RequestParam(required = false) String email) {

        if (userService.getUserByUsername(username) != null) {
            return Result.error("用户名已存在");
        }

        User user = userService.register(username, password, email);
        return Result.success("注册成功", user);
    }

    @PostMapping("/login")
    public Result<Map<String, Object>> login(
            @RequestParam String username,
            @RequestParam String password) {

        User user = userService.login(username, password);
        if (user == null) {
            return Result.error("用户名或密码错误");
        }

        String token = jwtTokenUtil.generateToken(user.getUserId(), user.getUsername());

        Map<String, Object> result = new HashMap<>();
        result.put("user", user);
        result.put("token", token);

        return Result.success("登录成功", result);
    }
}
```

UserService实现密码加密和验证:

```java
@Service
@RequiredArgsConstructor
public class UserService {

    private final UserMapper userMapper;
    private final PasswordEncoder passwordEncoder;

    public User register(String username, String password, String email) {
        User user = new User();
        user.setUsername(username);
        user.setPassword(passwordEncoder.encode(password));  // BCrypt加密
        user.setEmail(email);
        user.setStatus(1);

        userMapper.insert(user);
        user.setPassword(null);  // 移除密码字段
        return user;
    }

    public User login(String username, String password) {
        User user = userMapper.selectByUsername(username);
        if (user == null) {
            return null;
        }

        if (!passwordEncoder.matches(password, user.getPassword())) {
            return null;
        }

        user.setLastLoginAt(LocalDateTime.now());
        userMapper.updateById(user);

        user.setPassword(null);
        return user;
    }
}
```

#### 4.4.2 推荐服务调用实现

RecommendationService封装对Python算法服务的HTTP调用:

```java
@Service
@RequiredArgsConstructor
public class RecommendationService {

    private final RestTemplate restTemplate;

    @Value("${algorithm.service.url}")
    private String algorithmServiceUrl;

    public List<Map<String, Object>> getUserBasedRecommendations(Integer userId, Integer topN) {
        String url = algorithmServiceUrl + "/api/recommend/user-based";

        Map<String, Object> requestBody = new HashMap<>();
        requestBody.put("user_id", userId);
        requestBody.put("top_n", topN);

        try {
            ResponseEntity<Map> response = restTemplate.postForEntity(
                url, requestBody, Map.class
            );

            if (response.getStatusCode() == HttpStatus.OK) {
                Map<String, Object> body = response.getBody();
                if (body != null && (Boolean) body.get("success")) {
                    Map<String, Object> data = (Map<String, Object>) body.get("data");
                    return (List<Map<String, Object>>) data.get("recommendations");
                }
            }
        } catch (Exception e) {
            logger.error("调用推荐算法服务失败: {}", e.getMessage());
            return getPopularBooks(topN);  // 降级
        }

        return new ArrayList<>();
    }

    private List<Map<String, Object>> getPopularBooks(Integer topN) {
        // 从数据库查询热门图书作为降级方案
        List<Book> popularBooks = bookMapper.selectPopularBooks(topN);

        return popularBooks.stream()
            .map(book -> {
                Map<String, Object> map = new HashMap<>();
                map.put("bookId", book.getBookId());
                map.put("title", book.getTitle());
                map.put("author", book.getAuthor());
                map.put("avgRating", book.getAvgRating());
                return map;
            })
            .collect(Collectors.toList());
    }
}
```

RestTemplate配置超时时间：

```java
@Configuration
public class RestTemplateConfig {

    @Bean
    public RestTemplate restTemplate() {
        SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
        factory.setConnectTimeout(5000);   // 连接超时5秒
        factory.setReadTimeout(30000);     // 读取超时30秒
        return new RestTemplate(factory);
    }
}
```

#### 4.4.3 图书管理实现

BookController提供图书查询、搜索接口:

```java
@RestController
@RequestMapping("/books")
@RequiredArgsConstructor
public class BookController {

    private final BookService bookService;

    @GetMapping
    public Result<Page<Book>> getBooks(
            @RequestParam(defaultValue = "1") Integer page,
            @RequestParam(defaultValue = "20") Integer size) {

        Page<Book> bookPage = bookService.getBooks(page, size);
        return Result.success(bookPage);
    }

    @GetMapping("/search")
    public Result<List<Book>> searchBooks(@RequestParam String keyword) {
        List<Book> books = bookService.searchBooks(keyword);
        return Result.success(books);
    }

    @GetMapping("/{bookId}")
    public Result<Book> getBookDetail(@PathVariable String bookId) {
        Book book = bookService.getBookById(bookId);
        if (book == null) {
            return Result.error("图书不存在");
        }
        return Result.success(book);
    }
}
```

BookService使用MyBatis Plus查询:

```java
@Service
@RequiredArgsConstructor
public class BookService {

    private final BookMapper bookMapper;

    public Page<Book> getBooks(Integer page, Integer size) {
        Page<Book> pageInfo = new Page<>(page, size);
        return bookMapper.selectPage(pageInfo, null);
    }

    public List<Book> searchBooks(String keyword) {
        QueryWrapper<Book> wrapper = new QueryWrapper<>();
        wrapper.and(w -> w.like("title", keyword).or().like("author", keyword));
        wrapper.orderByDesc("avg_rating");
        return bookMapper.selectList(wrapper);
    }

    public List<Book> getPopularBooks(Integer limit) {
        QueryWrapper<Book> wrapper = new QueryWrapper<>();
        wrapper.ge("rating_count", 100)
               .ge("avg_rating", 4.0)
               .orderByDesc("avg_rating", "rating_count")
               .last("LIMIT " + limit);
        return bookMapper.selectList(wrapper);
    }
}
```

### 4.5 Web前端实现

Web前端基于Vue 3开发,采用Composition API,Element Plus构建UI。

#### 4.5.1 路由配置实现

router/index.js定义路由和守卫:

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import { useUserStore } from '@/stores/user'

const routes = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/Home.vue')
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue')
  },
  {
    path: '/books',
    name: 'BookList',
    component: () => import('@/views/BookList.vue')
  },
  {
    path: '/books/:id',
    name: 'BookDetail',
    component: () => import('@/views/BookDetail.vue')
  },
  {
    path: '/recommendations',
    name: 'Recommendations',
    component: () => import('@/views/Recommendations.vue'),
    meta: { requiresAuth: true }
  },
  {
    path: '/profile',
    name: 'Profile',
    component: () => import('@/views/Profile.vue'),
    meta: { requiresAuth: true }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫：未登录用户访问受保护页面时跳转登录
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()

  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    next({ name: 'Login' })
  } else {
    next()
  }
})

export default router
```

#### 4.5.2 状态管理实现

stores/user.js使用Pinia管理用户状态:

```javascript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useUserStore = defineStore('user', () => {
  const userId = ref(null)
  const username = ref('')
  const token = ref('')
  const userInfo = ref(null)

  const isLoggedIn = computed(() => !!token.value)

  function login(loginData) {
    userId.value = loginData.user.userId
    username.value = loginData.user.username
    token.value = loginData.token
    userInfo.value = loginData.user

    // 持久化到localStorage
    localStorage.setItem('userId', userId.value)
    localStorage.setItem('username', username.value)
    localStorage.setItem('token', token.value)
  }

  function logout() {
    userId.value = null
    username.value = ''
    token.value = ''
    userInfo.value = null

    localStorage.removeItem('userId')
    localStorage.removeItem('username')
    localStorage.removeItem('token')
  }

  function restoreFromLocalStorage() {
    const storedToken = localStorage.getItem('token')
    if (storedToken) {
      userId.value = localStorage.getItem('userId')
      username.value = localStorage.getItem('username')
      token.value = storedToken
    }
  }

  return {
    userId,
    username,
    token,
    userInfo,
    isLoggedIn,
    login,
    logout,
    restoreFromLocalStorage
  }
})
```

#### 4.5.3 HTTP请求封装实现

api/axios.js封装Axios,统一处理Token和错误:

```javascript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'
import router from '@/router'

const request = axios.create({
  baseURL: '/api',
  timeout: 30000
})

// 请求拦截器：自动添加Token
request.interceptors.request.use(
  config => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers['Authorization'] = `Bearer ${userStore.token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器：处理错误和Token过期
request.interceptors.response.use(
  response => {
    const res = response.data

    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')

      if (res.code === 401) {
        const userStore = useUserStore()
        userStore.logout()
        router.push('/login')
      }

      return Promise.reject(new Error(res.message || '请求失败'))
    }

    return res
  },
  error => {
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)

export default request
```

api/recommendation.js定义推荐API:

```javascript
import request from './axios'

export function getUserRecommendations(userId, topN = 10) {
  return request({
    url: `/recommendations/user-based/${userId}`,
    method: 'get',
    params: { topN }
  })
}

export function getSimilarBooks(bookId, topK = 6) {
  return request({
    url: `/recommendations/similar/${bookId}`,
    method: 'get',
    params: { topK }
  })
}
```

#### 4.5.4 推荐页面实现

views/Recommendations.vue展示个性化推荐:

```vue
<template>
  <div class="recommendations-page">
    <h1>个性化推荐</h1>

    <div v-if="loading" class="loading">
      <el-icon class="is-loading"><Loading /></el-icon>
      <p>正在为您生成个性化推荐...</p>
    </div>

    <div v-else-if="recommendations.length > 0" class="book-grid">
      <book-card
        v-for="book in recommendations"
        :key="book.bookId"
        :book="book"
      />
    </div>

    <el-empty v-else description="暂无推荐，快去评分图书吧" />
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useUserStore } from '@/stores/user'
import { getUserRecommendations } from '@/api/recommendation'
import BookCard from '@/components/BookCard.vue'
import { ElMessage } from 'element-plus'

const userStore = useUserStore()
const loading = ref(false)
const recommendations = ref([])

async function loadRecommendations() {
  loading.value = true
  try {
    const res = await getUserRecommendations(userStore.userId, 10)
    recommendations.value = res.data
  } catch (error) {
    ElMessage.error('加载推荐失败')
  } finally {
    loading.value = false
  }
}

onMounted(() => {
  loadRecommendations()
})
</script>

<style scoped>
.recommendations-page {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.book-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.loading {
  text-align: center;
  padding: 50px;
}
</style>
```

### 4.6 系统集成与部署

#### 4.6.1 服务启动流程

系统包含三个独立服务,启动顺序为：

**步骤1**：启动MySQL数据库服务（远程服务器已运行）。

**步骤2**：启动推荐算法服务。进入recommendation-algorithm-service目录,激活虚拟环境,执行：

```bash
venv\Scripts\activate
python app.py
```

服务启动在http://localhost:5000,全量加载数据到内存,耗时约10-30秒。日志显示"加载评分数据: 433671 条"、"加载图书数据: 271360 本"等信息,表示数据加载成功。

**步骤3**：启动后端服务。进入book-recommendation-backend目录,执行：

```bash
mvn spring-boot:run
```

或打包后运行：

```bash
mvn clean package
java -jar target/book-recommendation-backend-1.0.0.jar
```

服务启动在http://localhost:8080,自动连接远程数据库和本地算法服务。日志显示"Started BookRecommendationApplication"表示启动成功。

**步骤4**：启动前端服务。进入book-recommendation-frontend目录,执行：

```bash
npm run dev
```

开发服务器启动在http://localhost:3000,自动打开浏览器,显示首页。

生产部署时,前端执行`npm run build`生成dist静态文件,部署到Nginx；后端和算法服务使用Gunicorn、systemd等工具后台运行。

#### 4.6.2 配置管理

**后端application.yml配置**：

```yaml
spring:
  datasource:
    url: jdbc:mysql://116.205.244.106:3306/book_recommendation
    username: root
    password: 202358hjq
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

algorithm:
  service:
    url: http://localhost:5000

jwt:
  secret: your-secret-key-here-change-in-production
  expiration: 86400000  # 24小时
```

**前端vite.config.js代理配置**：

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

**算法服务config.py配置**：

```python
class Config:
    DB_HOST = '116.205.244.106'
    DB_PORT = 3306
    DB_USER = 'root'
    DB_PASSWORD = '202358hjq'
    DB_NAME = 'book_recommendation'

    CACHE_DIR = 'models/cache'
    CACHE_DURATION = 3600

    DEFAULT_TOP_N = 10
```

#### 4.6.3 系统集成验证

系统集成后,进行端到端测试验证功能完整性：

**测试1**：用户注册登录流程。访问http://localhost:3000,点击注册,填写用户名、密码,提交。系统调用后端/users/register接口,后端保存用户到数据库,返回用户信息。前端跳转登录页,输入用户名密码,调用/users/login接口,后端返回JWT Token,前端保存到localStorage,跳转首页。

**测试2**：图书浏览和搜索。首页展示热门图书,调用/books/popular接口,后端从数据库查询评分高且评分人数多的图书返回。点击搜索框,输入关键词"Harry Potter",调用/books/search接口,后端使用全文索引查询标题或作者包含关键词的图书,返回结果列表。

**测试3**：图书评分。进入图书详情页,点击星级评分组件选择4.5分,提交。前端调用/api/ratings接口,后端保存评分到ratings表,同时更新books表的avg_rating和rating_count字段。后端调用算法服务/api/cache/clear接口清除用户推荐缓存,调用/api/cache/precompute接口触发预计算。

**测试4**：个性化推荐。点击"推荐"菜单,访问推荐页面。前端调用/recommendations/user-based/{userId}接口,后端调用算法服务/api/recommend/user-based接口。算法服务检查缓存,如缓存未命中,执行混合推荐算法（70%协同过滤+30%内容推荐）,返回推荐列表。前端渲染图书卡片,展示推荐结果。

**测试5**：相似图书推荐。在图书详情页,展示"相似图书"模块。前端调用/recommendations/similar/{bookId}接口,后端调用算法服务/api/recommend/similar-items接口。算法服务执行物品协同过滤算法,计算图书相似度,返回Top-6相似图书。前端渲染相似图书列表。

所有测试通过,验证了系统各模块集成正确,功能完整。

---

**章节小结**

第3章详细阐述了系统的需求分析、总体设计、数据库设计和推荐算法设计。需求分析明确了功能性需求（用户管理、图书管理、评分管理、推荐系统）和非功能性需求（性能、可靠性、安全性、可维护性、可扩展性）。总体设计采用前后端分离架构和微服务模式,前端使用Vue 3,后端使用SpringBoot,推荐算法服务使用Python Flask。数据库设计定义了users、books、ratings三张核心表及其索引。推荐算法设计包括用户协同过滤、物品协同过滤、内容推荐和混合推荐策略的详细流程和优化方案。

第4章详细介绍了系统的实现过程。首先配置了前端（Node.js、VS Code、Vite）、后端（Java、IDEA、Maven）、算法服务（Python、PyCharm）的开发环境。数据预处理部分实现了Book-Crossing数据集的下载、清洗、转换和导入,最终获得433,671条评分、271,360本图书、77,805名用户的高质量数据。

推荐算法实现部分展示了数据加载模块、用户协同过滤、物品协同过滤、基于内容的推荐和混合推荐策略的完整代码。通过候选预筛选、向量化计算、缓存机制等优化手段,提升了算法性能。混合推荐策略根据用户状态动态选择算法组合,实现了全场景覆盖。

Web后端实现部分介绍了基于SpringBoot的用户认证（JWT+BCrypt）、推荐服务调用（RestTemplate+降级策略）、图书管理（MyBatis Plus）等核心功能。Web前端实现部分展示了基于Vue 3的路由配置、状态管理（Pinia）、HTTP请求封装（Axios拦截器）和推荐页面组件。

系统集成与部署部分说明了三层服务的启动流程、配置管理和端到端测试验证。整个系统采用前后端分离架构和微服务模式,各模块职责清晰,便于开发、测试和部署。

下一章将详细介绍系统测试与结果分析,包括测试环境配置、算法性能评估、功能测试、性能测试等内容,验证系统达到设计目标。

---

**字数统计**：第3章约15,200字,第4章约18,500字,合计约33,700字。
